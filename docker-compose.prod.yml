# Production Docker Compose для сервера
version: "2.2"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: vetguide-postgres-prod
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432" # Только локальный доступ
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
      - ./vetguide-api/scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    networks:
      - vetguide-prod-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 5
  # MinIO S3 Storage
  minio:
    image: minio/minio:latest
    container_name: vetguide-minio-prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:9000:9000" # Только локальный доступ
      - "127.0.0.1:9001:9001" # Только локальный доступ
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - minio_prod_data:/data
    networks:
      - vetguide-prod-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
  # MinIO Client for bucket setup
  minio-setup:
    image: minio/mc:latest
    container_name: vetguide-minio-setup-prod
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD};
      /usr/bin/mc mb myminio/${MINIO_BUCKET:-vetguide-uploads} --ignore-existing;
      /usr/bin/mc policy set public myminio/${MINIO_BUCKET:-vetguide-uploads};
      exit 0;
      "
    networks:
      - vetguide-prod-network

  # NestJS Application
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}/vetguide-api:latest
    container_name: vetguide-api-prod
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN:-7d}
      SMS_API_KEY: ${SMS_API_KEY}
      SMS_API_SECRET: ${SMS_API_SECRET}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET:-vetguide-uploads}
      MINIO_REGION: ${MINIO_REGION:-us-east-1}
      MINIO_USE_SSL: "false"
    volumes:
      - app_uploads_prod:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - vetguide-prod-network
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3001/api', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
  # Next.js UI Application
  ui:
    image: ghcr.io/${GITHUB_REPOSITORY}/vetguide-ui:latest
    container_name: vetguide-ui-prod
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: 1
    networks:
      - vetguide-prod-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: vetguide-nginx-prod
    restart: unless-stopped
    ports:
      - "8080:80"
      # - "443:443"  # Commented out due to port conflict with system nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/logs:/var/log/nginx
    depends_on:
      - ui
      - app
    networks:
      - vetguide-prod-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
volumes:
  postgres_prod_data:
    driver: local
  minio_prod_data:
    driver: local
  app_uploads_prod:
    driver: local

networks:
  vetguide-prod-network:
    driver: bridge

